import React from 'react';
import classes from './SkillsCategoryList.scss';

const skillsCategoryList = (props) => {
  return(
    <React.Fragment>

    </React.Fragment>
  );
};

export default skillsCategoryList;
