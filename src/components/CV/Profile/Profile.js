import React from 'react';
import classes from './Profile.scss';

const profile = (props) => {
  return(
    <React.Fragment>
      <h3>Profile</h3>
    </React.Fragment>
  );
};

export default profile;